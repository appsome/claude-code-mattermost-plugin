apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-code-bridge.fullname" . }}
  labels:
    {{- include "claude-code-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "claude-code-bridge.selectorLabels" . | nindent 4 }}
